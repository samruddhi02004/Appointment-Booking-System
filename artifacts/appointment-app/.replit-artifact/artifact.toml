kind = "web"
previewPath = "/"
title = "Appointment Booking System"
version = "1.0.0"
id = "artifacts/appointment-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20983

[services.development]
run = "pnpm --filter @workspace/appointment-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/appointment-app", "run", "build" ]
publicDir = "artifacts/appointment-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20983"
BASE_PATH = "/"
